# Calorie Tracker

A simple web application to display the latest calorie count from a CSV file.

## How to run

1.  **Install dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Run the server:**
    ```bash
    python server.py
    ```

3.  **Open your browser** and navigate to `http://localhost:5000`.
